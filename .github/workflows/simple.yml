---
name: "Terraform Setup"
on: pull_request
jobs: 
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: CheckOut
        uses: actions/checkout@v1
      - name: Add base Terraform File
        run: touch main.tf

      - uses: hashicorp/setup-terraform@v1
      - run: terraform init
      - id: plan
        run: terraform plan -no-color

      - name: List files
        run: |
          pwd
          ls -ls
          uptime

      #   name: "echo String"
      #   run: "echo \"Hello From First Github actions\""
      # - 
      #   name: multiline
      #   run: |-
      #       npm -v
      #       curl ifconfig.me
      # - name: python command
      #   run: |
      #     import platform
      #     print (platform.processor())
      #   shell: python

  # run-windows-command:
  #   runs-on: windows-latest
  #   steps:
  #     -
  #       name: "Directory Powershell"
  #       run: pwd
  #       shell: bash
